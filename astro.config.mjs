// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://anthoro.github.io', // TODO: update with your real domain before deploy
  output: 'static',
});
