// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://ta-chwila.netlify.app', // update if your Netlify URL differs
  output: 'static',
});
